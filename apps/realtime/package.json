{
  "name": "realtime",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node --import tsx src/index.ts",
    "test:client": "bun run scripts/test-client.ts",
    "test:subscriber": "bun run scripts/redis-subscriber.ts",
    "test:stress": "bun run scripts/stress-test.ts",
    "lint": "biome check --log-level=info --diagnostic-level=warn .",
    "lint:fix": "biome check --write --unsafe .",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsx": "^4.21.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ioredis": "^5.9.0",
    "uWebSockets.js": "https://github.com/uNetworking/uWebSockets.js/archive/v20.56.0.tar.gz"
  }
}
