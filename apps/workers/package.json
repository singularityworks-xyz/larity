{
  "name": "workers",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "LOG_LEVEL=fatal bun test --preload ./tests/setup.ts",
    "lint": "biome check --log-level=info --diagnostic-level=warn .",
    "lint:fix": "biome check --write --unsafe .",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^6"
  },
  "dependencies": {
    "@larity/infra": "workspace:*",
    "@larity/jobs": "workspace:*",
    "@larity/logger": "workspace:*",
    "@larity/telemetry": "workspace:*",
    "bullmq": "^5.48.1",
    "elysia": "^1.4.25",
    "ioredis": "^5.9.0"
  }
}
