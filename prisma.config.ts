// This file was generated by Prisma, and assumes you have installed the following:
// npm install --save-dev prisma dotenv

import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { config } from 'dotenv';
import { defineConfig } from 'prisma/config';

const __dirname = path.dirname(fileURLToPath(import.meta.url));

config({ path: path.resolve(__dirname, '.env') }); // loads .env
config({ path: path.resolve(__dirname, '.env.development') }); // loads .env.development if it exists

export default defineConfig({
  schema: './packages/infra/prisma/schema.prisma',
  migrations: {
    path: './packages/infra/prisma/migrations',
  },
  datasource: {
    url: process.env.DATABASE_URL,
  },
});
